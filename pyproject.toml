[project]
name = "desktop2steoro-vulkan"
version = "0.1.0"
description = "Python Vulkan runtime for Desktop2Stereo"
requires-python = ">=3.11,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 120

